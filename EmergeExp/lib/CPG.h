#include <math.h>

float teta;
float ampli;
float dampli;
float offset;
float doffset;
float output;
float ampliSet;
float offsetSet;
float phaseDiffSet;
float dt;

void updateCPG(float teta1,float teta2);