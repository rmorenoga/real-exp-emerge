#include <math.h>

float teta[5];
float ampli;
float dampli;
float offset;
float doffset;
float output;
float ampliSet;
float offsetSet;
float phaseDiffSet[2];
float dt;

void updateCPG(float teta[]);